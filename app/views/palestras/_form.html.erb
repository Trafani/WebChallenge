<%= form_with(model: palestra) do |form| %>
  <% if palestra.errors.any? %>
    <div class="alert alert-danger">
      <h4><%= pluralize(palestra.errors.count, "error") %> prohibited this palestra from being saved:</h4>
      <ul>
        <% palestra.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :nome, 'Nome', class: 'form-label' %>
    <%= form.text_field :nome, class: 'form-input' %>
  </div>

  <div class="form-group">
    <%= form.label :duracao, 'Duração (minutos)', class: 'form-label' %>
    <%= form.number_field :duracao, class: 'form-input', min: 5 %>
  </div>

  <div class="form-group">
    <%= form.submit 'Salvar', class: 'btn btn-primary' %>
  </div>
<% end %>
